# EXIT SCRIPT ###################################
import os
import time
import threading
def kill_prog():
    time.sleep(5)
    print('Program killed after being unresposive.')
    os._exit(THREADING_TIMER)
kill_thread = threading.Thread(target=kill_prog)
kill_thread.start()
# ###############################################

